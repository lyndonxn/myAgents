# DeepSeek API（OpenAI 兼容）
# 复制本文件为 .env 并填入你的 Key： cp .env.example .env
DEEPSEEK_API_KEY=replace-with-your-api-key

# 可选覆盖：默认 https://api.deepseek.com，模型在 config.yaml 中配置
# DEEPSEEK_BASE_URL=https://api.deepseek.com
